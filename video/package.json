{
  "name": "causalist-video",
  "private": true,
  "version": "1.1.0",
  "description": "Demo video cutting scenes for Causalist (Built-with-Opus 4.7 hackathon).",
  "scripts": {
    "studio": "remotion studio",
    "render:final": "mkdir -p out/final && for c in cold-open logo-draws-as-graph the-problem the-pivot bringing-causality-to-agents agents-on-graph claude-code-builds-for-you closing; do remotion render src/index.ts $c out/final/$c.mp4; done",
    "render:alt-opener": "mkdir -p out/final && remotion render src/index.ts logo-draws-as-graph-alt out/final/logo-draws-as-graph-alt.mp4",
    "render": "remotion render src/index.ts",
    "still": "remotion still src/index.ts"
  },
  "dependencies": {
    "@remotion/cli": "^4.0.300",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "remotion": "^4.0.300"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "typescript": "^5.6.3"
  }
}
